require("dotenv").config();

const dataPolicy = {
  userAcceptedDataPolicy: true,
  //TODO: check out your issue on dotenv repo for boolean expressions in .env files
};

module.exports = { dataPolicy };
